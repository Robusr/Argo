# Argo 项目 Git 提交规范
## 1. 提交信息格式
提交信息需简洁明了、规范统一，采用以下格式（冒号后加空格）：
```
<type>(<scope>): <subject>
[空行]
[body]
[空行]
[footer]
```
### 1.1 各部分说明
- type（必选）：提交类型，描述提交的核心操作，仅允许以下值：
  - feat：新增功能（如“新增Matlab水动力学仿真模块”）
  - fix：修复bug（如“修复上位机串口通信卡顿问题”）
  - docs：文档修改（如“更新Software模块README.md”）
  - style：代码风格调整（不影响功能，如缩进、命名规范修正）
  - refactor：代码重构（不新增功能、不修复bug，如优化函数逻辑）
  - test：新增/修改测试代码（如“新增STM32电机控制测试用例”）
  - chore：构建/依赖/工具相关操作（如“更新requirements.txt依赖版本”）
  - perf：性能优化（如“优化仿真代码运行速度”）
  - revert：回滚提交（如“revert: feat(control): 回滚PID控制功能”）
- scope（必选）：提交所属模块，明确修改范围，仅允许以下值：
  - analysis：仿真分析模块（Matlab/Ansys）
  - software：上位机软件模块
  - hardware：硬件嵌入式模块（STM32/树莓派/PCB）
  - drawings：三维建模模块
  - root：根目录文件（如README.md、规范文档）
- subject（必选）：提交简要描述，100字符以内，简洁明了，首字母小写，结尾不加句号
- body（可选）：提交详细说明，描述修改目的、实现细节、修改逻辑，换行清晰
- footer（可选）：备注信息，如关联Issue（“Closes #123”）、不兼容变更（“BREAKING CHANGE: 接口参数调整”）

### 1.2 示例
```
feat(software): 新增上位机数据可视化图表功能
- 实现传感器数据实时折线图显示
- 支持数据导出为csv格式
- 优化图表响应速度，解决卡顿问题
Closes #45
```

## 2. 提交规范补充
1. 每次提交仅对应一个功能/ bug修复/文档修改，避免一次提交多个不相关修改
2. 提交前需自行测试，确保无语法错误、功能正常，避免提交无效代码
3. 禁止提交编译产物、临时文件、敏感信息，遵循DEV_SPEC.md中的文件提交要求
4. 提交信息需使用中文，避免英文（专业术语除外），确保团队所有成员可理解

## 3. 分支管理规范（含dev分支专项）
### 3.1 核心分支定义（固定分支，禁止删除）
- main：主分支，存放稳定可运行的正式版本代码，仅通过PR从dev分支合并，禁止直接提交
- dev：开发分支，核心开发分支，存放正在开发的功能集成代码，团队所有开发工作均基于dev分支展开，禁止直接提交，需通过feature/bugfix分支合并

### 3.2 临时分支定义（开发完成后删除）
- feature/xxx：功能分支，用于开发新增功能，基于dev分支创建，命名格式：feature/模块-功能描述（如feature/software-data-visualization）
- bugfix/xxx：bug修复分支，用于修复dev分支或main分支的bug，基于对应分支创建，命名格式：bugfix/模块- bug描述（如bugfix/hardware-stm32-uart-error）
- hotfix/xxx：紧急修复分支，用于修复main分支的紧急bug（不影响dev分支开发），基于main分支创建，修复后同步合并到dev和main分支，命名格式：hotfix/bug描述（如hotfix/software-crash）
- release/xxx：版本发布分支，用于版本迭代准备，基于dev分支创建，命名格式：release/v主版本号.次版本号.修订号（如release/v1.0.0），测试稳定后合并到main和dev分支
- 根据具体情况，会临时改变分支规则

### 3.3 dev分支专项管理规范
1. dev分支为团队开发核心分支，所有新功能、bug修复（非紧急）均需通过临时分支合并到dev，禁止直接向dev分支提交代码
2. 每次向dev分支提交PR前，需确保分支已同步dev最新代码，解决所有冲突，且代码符合DEV_SPEC.md规范
3. dev分支需保持可运行状态，禁止合并未测试、存在语法错误的代码，提交PR后需经过至少1名团队成员审核通过方可合并
4. 每周至少对dev分支进行1次代码评审，检查代码规范、功能完整性，及时发现并解决潜在问题
5. 当dev分支集成的功能达到版本迭代要求时，从dev分支创建release分支，进行版本测试和优化，最终合并到main分支

### 3.4 分支操作流程（核心）
1. 开发新功能：克隆仓库 → 切换到dev分支 → 拉取最新代码 → 创建feature分支 → 开发 → 提交（遵循提交规范） → 推送分支 → 提交PR到dev分支 → 审核通过后合并 → 删除feature分支
2. 修复dev分支bug：切换到dev分支 → 拉取最新代码 → 创建bugfix分支 → 修复bug → 提交 → 推送 → 提交PR到dev分支 → 审核通过后合并 → 删除bugfix分支
3. 紧急修复main分支bug：切换到main分支 → 拉取最新代码 → 创建hotfix分支 → 修复bug → 提交 → 推送 → 提交PR到main分支 → 审核通过后合并 → 同步合并到dev分支 → 删除hotfix分支
4. 版本发布：切换到dev分支 → 拉取最新代码 → 创建release分支 → 版本优化/测试 → 提交PR到main分支 → 审核通过后合并 → 同步合并到dev分支 → 删除release分支

## 4. PR（Pull Request）规范
1. PR标题需遵循“<type>(<scope>): <subject>”格式，与提交信息一致
2. PR描述需包含：修改目的、实现细节、测试结果、涉及文件
3. PR提交前需确保代码符合DEV_SPEC.md规范，无语法错误、测试通过
4. 每个PR仅对应一个功能或一个bug修复，避免多用途PR
5. PR审核通过后，由提交者负责合并，合并后删除对应分支

## 5. Issue规范
1. Issue标题需简洁明确，说明问题或需求（如“上位机数据显示延迟”“需新增Ansys船体强度分析”）
2. Issue描述需包含：
   - 问题类：问题现象、复现步骤、预期结果、实际结果
   - 需求类：需求用途、具体功能描述、实现建议
3. 为Issue添加对应标签（如bug、feature、documentation），便于分类管理
4. 问题解决后，及时关闭Issue，并在关闭时说明解决方式

## 6. 语义化版本规范
版本号格式：主版本号.次版本号.修订号（如v1.0.0）
- 主版本号（Major）：重大功能变更、不兼容的API修改（如整体架构调整）
- 次版本号（Minor）：新增功能、兼容的API扩展（如新增仿真模块功能）
- 修订号（Patch）：bug修复、小优化（不影响功能使用）