# Argo 项目GitHub仓库文件结构规范
## 概述
本文档定义了 "Argo" 船设计项目的 GitHub 仓库整体文件结构规范。所有团队成员必须严格遵循此结构进行代码提交和文件管理，确保仓库整洁、可维护，便于团队协作和项目迭代。

## 完整目录树结构
```
Argo/
├── .gitignore                    # Git忽略文件配置
├── .gitattributes                # Git属性配置（LFS等）
├── LICENSE                       # 开源许可证
├── README.md                     # 项目主说明文档
├── COMMIT_SPEC.md                # Git提交规范（含dev分支管理）
├── DEV_SPEC.md                   # 开发规范
├── FILE_STRUCTURE.md             # 本文档（文件结构规范）
│
├── Analysis/                     # 仿真分析模块
│   ├── README.md                 # 仿真模块说明文档
│   ├── Matlab/                   # Matlab仿真代码
│   │   ├── src/                  # 源代码目录
│   │   │   ├── hydrodynamics/    # 水动力学仿真（船体受力、运动仿真）
│   │   │   ├── control/          # 控制算法仿真（PID控制、路径规划等）
│   │   │   ├── navigation/       # 导航算法仿真（定位、避障等）
│   │   │   └── utils/            # 工具函数（数据转换、参数计算等）
│   │   ├── scripts/              # 运行脚本（一键启动仿真、批量处理数据）
│   │   ├── data/                 # 输入数据文件（船体参数、环境参数等）
│   │   └── results/              # 仿真结果输出
│   │       ├── figures/          # 图表文件（仿真曲线、结果可视化图）
│   │       └── logs/             # 日志文件（仿真过程记录、参数日志）
│   ├── Ansys/                    # Ansys有限元分析（船体强度、结构仿真）
│   │   ├── project_files/        # Ansys项目文件（.wbpj等）
│   │   ├── models/               # 导入模型（从SolidWorks导出的STEP文件）
│   │   ├── meshes/               # 网格文件（网格划分结果）
│   │   ├── results/              # 分析结果（应力、应变等数据文件）
│   │   └── reports/              # 分析报告（仿真结论、数据汇总）
│   └── docs/                     # 仿真相关文档（理论依据、参数说明、算法文档）
│
├── Software/                     # 上位机软件模块（PC端控制、数据显示）
│   ├── README.md                 # 软件模块说明文档
│   ├── src/                      # 源代码目录
│   │   ├── main/                 # 主程序入口（程序启动、初始化）
│   │   ├── ui/                   # 用户界面模块（界面布局、控件逻辑）
│   │   ├── communication/        # 通信模块（串口/网络通信，与硬件交互）
│   │   ├── data_processing/      # 数据处理模块（传感器数据解析、滤波）
│   │   ├── visualization/        # 可视化模块（数据图表、船体状态显示）
│   │   ├── config/               # 配置文件（通信参数、显示参数等）
│   │   └── utils/                # 工具类（日志记录、异常处理、文件操作）
│   ├── tests/                    # 单元测试（各模块功能测试、接口测试）
│   ├── resources/                # 资源文件
│   │   ├── icons/                # 图标资源（程序图标、控件图标）
│   │   ├── images/               # 图片资源（背景图、船体示意图）
│   │   ├── fonts/                # 字体资源（界面显示字体）
│   │   └── styles/               # 样式文件（界面主题、控件样式）
│   ├── docs/                     # 软件文档（用户手册、开发文档、接口说明）
│   ├── scripts/                  # 构建和部署脚本（一键打包、环境配置）
│   ├── requirements.txt          # Python依赖包清单（版本明确）
│   └── setup.py                  # 打包配置文件（支持生成可执行文件）
│
├── Hardware/                     # 硬件与嵌入式模块（控制核心、硬件电路）
│   ├── README.md                 # 硬件模块说明文档
│   ├── Firmware/                 # 固件代码（嵌入式设备程序）
│   │   ├── STM32/                # STM32嵌入式代码（主控制核心）
│   │   │   ├── Core/             # 核心代码
│   │   │   │   ├── Inc/          # 头文件（自定义头文件、外设头文件）
│   │   │   │   ├── Src/          # 源文件（主函数、外设驱动、控制逻辑）
│   │   │   │   └── Startup/      # 启动文件（STM32启动代码）
│   │   │   ├── Drivers/          # 驱动文件（GPIO、UART、SPI等外设驱动）
│   │   │   ├── Middlewares/      # 中间件（FreeRTOS、USB等）
│   │   │   └── MDK-ARM/          # MDK工程文件（可直接编译）
│   │   ├── RaspberryPi/          # 树莓派代码（辅助控制、数据处理）
│   │   │   ├── src/              # Python/C源码（传感器读取、通信转发）
│   │   │   ├── config/           # 配置文件（引脚配置、通信参数）
│   │   │   └── scripts/          # 运行脚本（一键启动、自启动配置）
│   │   └── common/               # 通用协议和库（STM32与树莓派通信协议、共用函数）
│   ├── PCB/                      # PCB设计文件（硬件电路设计）
│   │   ├── project/              # Altium/KiCad工程文件
│   │   ├── schematics/           # 原理图文件（电路连接图）
│   │   ├── layout/               # PCB布局文件（电路板布局）
│   │   ├── libraries/            # 元件库（自定义元件、常用元件）
│   │   ├── gerber/               # 生产文件（用于PCB打样）
│   │   ├── BOM.xlsx              # 物料清单（元器件型号、数量、封装）
│   │   └── docs/                 # 设计文档（设计说明、电路原理、注意事项）
│   └── docs/                     # 硬件相关文档
│       ├── datasheets/           # 芯片手册（STM32、传感器、芯片等手册）
│       ├── schematics.pdf        # 原理图PDF版（便于查阅）
│       └── wiring_diagram.pdf    # 接线图（硬件接线说明、引脚定义）
│
└── Drawings/                     # 三维建模与工程图（船体结构、零件建模）
    ├── README.md                 # 建模模块说明文档
    ├── SolidWorks/               # SolidWorks源文件
    │   ├── parts/                # 零件文件（单个零件建模）
    │   │   ├── hull/             # 船体零件（船身、甲板等）
    │   │   ├── propulsion/       # 推进系统（电机、螺旋桨等）
    │   │   ├── electronics/      # 电子安装件（传感器支架、电路板固定件）
    │   │   └── structure/        # 结构件（支架、连接件等）
    │   ├── assemblies/           # 装配体文件（零件组装）
    │   │   ├── main_assembly.SLDASM  # 总装配体（所有零件组装完成）
    │   │   └── sub_assemblies/   # 子装配体（局部零件组装，如推进系统装配）
    │   ├── drawings/             # 工程图（零件图、装配图，用于加工）
    │   └── templates/            # 模板文件（SolidWorks模板、图纸模板）
    ├── Exported/                 # 导出文件（跨平台通用格式）
    │   ├── STEP/                 # STEP通用格式（用于Ansys导入、跨软件编辑）
    │   ├── STL/                  # 3D打印文件（零件3D打印）
    │   ├── DXF/                  # CAD交换格式（用于其他CAD软件编辑）
    │   └── PDF/                  # PDF工程图（便于查阅、打印）
    └── docs/                     # 建模相关文档
        ├── assembly_guide.md     # 装配说明（零件装配步骤、注意事项）
        └── bom.xlsx              # 结构件BOM（零件型号、数量、材质）
```

## 根目录必要文件说明
### 1. 配置文件
| 文件名 | 说明 |
|--------|------|
| `.gitignore` | 定义 Git 需要忽略的文件类型，包括编译产物、临时文件、日志、大文件等，避免冗余文件提交 |
| `.gitattributes` | 配置 Git LFS 追踪的大文件类型（SolidWorks、Ansys 文件等），确保大文件正常提交和拉取 |
| `LICENSE` | 项目开源许可证，建议使用 MIT License，明确项目使用权限 |

### 2. 规范文档
| 文件名 | 说明 |
|--------|------|
| `README.md` | 项目主说明文档，包含项目介绍、技术栈、快速开始、团队协作等核心信息 |
| `COMMIT_SPEC.md` | Git 提交规范、分支管理（含dev分支）、PR/Issue 规范等，规范团队提交行为 |
| `DEV_SPEC.md` | 开发规范、代码风格、命名规范等，确保代码和文件的规范性 |
| `FILE_STRUCTURE.md` | 本文档，定义仓库文件结构，明确各目录和文件的用途 |

## 各模块目录说明
### 1. Analysis 模块
- `Matlab/`：所有 Matlab 仿真相关代码，按功能（水动力学、控制、导航）分子目录，便于分类管理和复用
- `Ansys/`：有限元分析项目文件和结果，按流程（项目文件、模型、网格、结果）存放，确保分析过程可追溯
- `docs/`：仿真理论依据、参数说明、算法文档等，便于团队理解仿真逻辑和参数设置

### 2. Software 模块
- 按功能模块组织源代码，分离主程序、UI、通信、数据处理等模块，降低耦合度
- 分离测试代码（tests）和资源文件（resources），确保代码结构清晰
- 包含构建和部署脚本，支持一键打包和环境配置，提高开发效率

### 3. Hardware 模块
- 分离不同平台的固件代码（STM32、树莓派），避免代码混淆，便于单独开发和调试
- PCB 文件包含完整工程和生产输出（gerber文件），支持直接用于PCB打样
- 芯片手册、原理图、接线图统一管理，便于查阅和硬件调试

### 4. Drawings 模块
- SolidWorks 文件按零件、装配体、工程图分类，清晰区分不同类型的建模文件
- 导出通用格式文件（STEP、STL、PDF），便于跨平台使用（如Ansys导入、3D打印）
- 装配说明和结构件 BOM 单独管理，便于零件加工和装配

## 目录命名规范
1. 文件夹命名：使用 PascalCase（大驼峰）命名主文件夹，如 `Analysis`、`Software`、`Hardware`、`Drawings`
2. 子文件夹命名：使用 snake_case（下划线小写），如 `hydrodynamics`、`data_processing`、`stm32`
3. 避免空格和特殊字符：所有目录名不包含空格、中文、特殊符号（如@、#、$），避免兼容性问题
4. 保持简洁：目录名简洁明了，不超过 20 个字符，便于识别和输入

## 文件管理原则
1. 单一职责：每个目录只存放相关类型的文件，避免文件混杂（如仿真代码不放入硬件目录）
2. 大小限制：超过 100MB 的文件必须使用 Git LFS 追踪，避免仓库体积过大，影响克隆和拉取速度
3. 禁止提交：
   - 编译生成的二进制文件（如.exe、.bin、.hex）
   - 临时文件和缓存（如Matlab缓存、SolidWorks临时文件）
   - 密码、密钥等敏感信息
   - OS 生成的隐藏文件（.DS_Store、Thumbs.db 等）
4. 文档同步：代码更新时同步更新对应 README 文档和相关说明文档，确保文档与代码一致